

    ## loop: 1 iteration = one path
    for(i in 1:N){
        objName <- paste("path",vec.lab[i],sep="")
        assign(objName, dijkstraFrom(hgdp, startNode))
        myPath <- get(objName)
        save(objName, file=paste(objName,".RData",sep=""))

        geo.dist <- sapply(myPath[-length(myPath)],function(e) e$length)
        R2[i] <- cor(geo.dist, hgdp@data$Genetic.Div)^2
    }


    ## return result
    names(R2) <- startNode
    return(R2)

} # end doSimul

doSimul(ori, "/home/master/Desktop/")
doSimul <- function(startNode,path=""){
    ## set path
    curwd <- getwd()
    setwd(path)
    on.exit(setwd(curwd))

    ## some variables
    N <- length(startNode)
    vec.lab <- .genlab("",N)
    R2 <- numeric(N)


    ## loop: 1 iteration = one path
    for(i in 1:N){
        objName <- paste("path",vec.lab[i],sep="")
        assign(objName, dijkstraFrom(hgdp, startNode))
        myPath <- get(objName)
        save(get(objName), file=paste(objName,".RData",sep=""))

        geo.dist <- sapply(myPath[-length(myPath)],function(e) e$length)
        R2[i] <- cor(geo.dist, hgdp@data$Genetic.Div)^2
    }


    ## return result
    names(R2) <- startNode
    return(R2)

} # end doSimul

doSimul(ori, "/home/master/Desktop/")
doSimul <- function(startNode,path=""){
    ## set path
    curwd <- getwd()
    setwd(path)
    on.exit(setwd(curwd))

    ## some variables
    N <- length(startNode)
    vec.lab <- .genlab("",N)
    R2 <- numeric(N)


    ## loop: 1 iteration = one path
    for(i in 1:N){
        objName <- paste("path",vec.lab[i],sep="")
        assign(objName, dijkstraFrom(hgdp, startNode))
        myPath <- get(objName)
        save(myPath, file=paste(objName,".RData",sep=""))

        geo.dist <- sapply(myPath[-length(myPath)],function(e) e$length)
        R2[i] <- cor(geo.dist, hgdp@data$Genetic.Div)^2
    }


    ## return result
    names(R2) <- startNode
    return(R2)

} # end doSimul

doSimul(ori, "/home/master/Desktop/")
plot(worldgraph)
plot(worldgraph.40k)
doSimul(c('1','2'), "/home/master/Desktop/")
doSimul <- function(startNode,path=""){
    ## set path
    curwd <- getwd()
    setwd(path)
    on.exit(setwd(curwd))

    ## some variables
    N <- length(startNode)
    vec.lab <- .genlab("",N)
    R2 <- numeric(N)


    ## loop: 1 iteration = one path
    for(i in 1:N){
        objName <- paste("path",vec.lab[i],sep="")
        assign(objName, dijkstraFrom(hgdp, startNode))
        myPath <- get(objName)
        save(myPath, file=paste(objName,".RData",sep=""))

        geo.dist <- sapply(myPath[-length(myPath)],function(e) e$length)
        R2[i] <- cor(geo.dist, hgdp@data$Genetic.Div)^2
    }


    ## return result
    names(R2) <- startNode
    return(R2)

} # end doSimul

plot(worldgraph.40k)
data(worldgraph.40k)
source("/home/master/dev/geograph/pkg/misc/simulations/simulations.1.0.R")
q()
n
source("/home/master/dev/geograph/pkg/misc/simulations/simulations.1.0.R")
source("/home/master/dev/geograph/pkg/misc/simulations/simulations.1.0.R")
source("/home/master/dev/geograph/pkg/misc/simulations/simulations.1.0.R")
plot(worldgraph.40k)
?isInArea
isInArea(worldgraph.40k,locator(2),res="character")
doSimul(c("21820","22459"), "outputs")
load("/home/master/dev/geograph/pkg/misc/simulations/outputs/path2.RData")
plot(myPath)
worldgraph.40k@meta$costs
worldgraph.40k@meta$costs <- edit(worldgraph.40k@meta$costs)
doSimul(c("21820","22459"), "outputs")
load("/home/master/dev/geograph/pkg/misc/simulations/outputs/path2.RData")
plot(myPath)
plot(worldgraph.40k)
plot(myPath)
worldgraph.40k@meta$costs <- edit(worldgraph.40k@meta$costs)
save(worldgraph.40k, file="/home/master/dev/geograph/pkg/data/worldgraph.40k.RData")
q()
n
source("/home/master/dev/geograph/pkg/misc/simulations/simulations.1.0.R")
 doSimul(c("21820","22459"), "outputs")

load("/home/master/dev/geograph/pkg/misc/simulations/outputs/path1.RData")
plot(worldgraph.40k)
plot(myPath)
worldgraph.40k@meta$costs <- edit(worldgraph.40k@meta$costs)
save(worldgraph.40k, file="/home/master/dev/geograph/pkg/data/worldgraph.40k.RData")
q()
n
